import { StubPage } from "../StubPage";

export function OpenAccountPage() {
  return <StubPage title="Open an Account" lede="Apply with a referral code. Demo only — no real account is opened." />;
}
