import { StubPage } from "../StubPage";

export function UsdtPage() {
  return <StubPage title="USDT Wallet" lede="A separately-governed digital-asset balance, distinct from the fiat currency ledgers." />;
}
