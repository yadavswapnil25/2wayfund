# Base URL of the 2wayfund-API Laravel backend.
VITE_API_URL=http://127.0.0.1:8000/api/v1

# Shared secret sent as the X-Client-Key header on every API request.
# Must match the backend's CLIENT_API_KEY (see 2wayfund-API/.env).
VITE_CLIENT_API_KEY=changeme
